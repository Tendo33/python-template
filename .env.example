# Environment Configuration for Python Template
# Copy this file to .env and customize as needed
# 将此文件复制为 .env 并根据需要自定义

# ============================================================================
# Application Settings / 应用程序配置
# ============================================================================

# Application name / 应用名称
APP__NAME=python-template

# Application version / 应用版本
APP__VERSION=0.1.0

# Debug mode (true/false) / 调试模式
APP__DEBUG=false

# Environment (development/staging/production) / 环境
APP__ENVIRONMENT=development


# ============================================================================
# Logging Settings / 日志配置
# ============================================================================

# Logging level (TRACE/DEBUG/INFO/SUCCESS/WARNING/ERROR/CRITICAL) / 日志级别
LOGGING__LEVEL=INFO

# Log format (standard/json) / 日志格式
LOGGING__FORMAT=standard

# Log file path (empty for stdout only) / 日志文件路径（留空则仅输出到标准输出）
LOGGING__FILE=

# Log rotation size / 日志轮转大小
LOGGING__ROTATION=10 MB

# Log retention period / 日志保留期限
LOGGING__RETENTION=1 week

# Use JSON format for logs (true/false) / 使用JSON格式日志
LOGGING__JSON_FORMAT=false


# ============================================================================
# Performance Settings / 性能配置
# ============================================================================

# Request timeout in seconds / 请求超时时间（秒）
PERFORMANCE__TIMEOUT=30

# Maximum retry attempts / 最大重试次数
PERFORMANCE__MAX_RETRIES=3

# Buffer size in bytes / 缓冲区大小（字节）
PERFORMANCE__BUFFER_SIZE=1024

# Number of concurrent workers / 并发工作线程数
PERFORMANCE__CONCURRENT_WORKERS=4


# ============================================================================
# Security Settings / 安全配置
# ============================================================================

# Secret key for encryption (REQUIRED in production!) / 加密密钥（生产环境必需！）
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECURITY__SECRET_KEY=

# API key for external services / 外部服务API密钥
SECURITY__API_KEY=

# Verify SSL certificates (true/false) / 验证SSL证书
SECURITY__SSL_VERIFY=true

# Allowed hosts (comma-separated) / 允许的主机（逗号分隔）
SECURITY__ALLOWED_HOSTS=localhost,127.0.0.1


# ============================================================================
# Notes / 注意事项
# ============================================================================
#
# 1. Environment variables take precedence over .env file values
#    环境变量优先于 .env 文件中的值
#
# 2. Use double underscores (__) to set nested configuration
#    使用双下划线 (__) 设置嵌套配置
#    Example: APP__NAME sets settings.app.name
#
# 3. Never commit .env file to version control
#    永远不要将 .env 文件提交到版本控制
#
# 4. For production, set sensitive values via environment variables
#    在生产环境中，通过环境变量设置敏感值
#
